This trigger runs after update on Appointment__c records.
Compares the current status (Status__c) with the old status.
It triggers an email notification when the status has changed to "Confirmed" or "Cancelled."
