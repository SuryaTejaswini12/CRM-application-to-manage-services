The trigger runs after an update on Application__c records.
It compares the old and new values of the Status__c field to determine if there was a change.
If the status has changed, it proceeds to create an email notification.
